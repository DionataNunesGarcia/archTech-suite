apiVersion: v1
kind: Service
metadata:
  name: {{ include "archnet.fullname" . }}
  labels:
    {{- include "archnet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "archnet.selectorLabels" . | nindent 4 }}
